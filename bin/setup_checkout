
CARROT="~/carrot"
cd $CARROT/customers/store
./script/setup

#(Every time you run into issues after pull from master, run this command)
sudo chown -R $USER /Library/Ruby
cd $CARROT/customers/instacart/
script/update

#Seed data
#(94105 is zip, 1 is warehouse id)
cd $CARROT/customers/instacart/
./script/seed 94105 1

Sync access/permissions to your local table get your login working
pgsync gatekeeper
(Sync for new accounts happens on Thursday)

